# 第 8 章：技能系统（Skills）—— 让 AI 学会专业本领

> 通用 Agent 什么都能做一点，但什么都不精。技能系统让 Agent 在特定领域拥有**专家级能力**——就像给一个聪明的实习生配上一本专业手册。

---

## 🔍 先看 Claude Code 怎么做

Claude Code 的技能系统是整个平台最强大的扩展机制之一。它让任何人都可以编写一个 `SKILL.md` 文件，然后 Agent 就能获得新的专业能力。

### 技能文件结构

Claude Code 的技能目录结构非常简洁：

```
.claude/skills/
├── code-review/
│   └── SKILL.md        # 代码审查技能
├── data-analysis/
│   └── SKILL.md        # 数据分析技能
└── article-writing/
    └── SKILL.md        # 文章写作技能
```

每个技能就是一个目录 + 一个 `SKILL.md` 文件。目录名就是技能名。

### 技能文件格式（SKILL.md）

每个 `SKILL.md` 包含 **YAML frontmatter** + **Markdown 正文**：

```markdown
---
name: code-review
description: Perform thorough code review with best practices
when_to_use: When reviewing pull requests or code changes
allowed-tools: Read, Grep, Glob
---

# Code Review Skill

You are an expert code reviewer. Follow these steps:

1. Read the changed files
2. Check for security issues
3. Review architecture decisions
4. Suggest improvements
...
```

### 技能加载流程

参考 `src/skills/loadSkillsDir.ts`，加载过程：

```
1. 扫描多个目录：
   - ~/.claude/skills/          (用户级)
   - .claude/skills/            (项目级)
   - 管理策略目录

2. 对每个子目录：
   - 读取 SKILL.md
   - 解析 frontmatter（YAML 元数据）
   - 解析 Markdown 正文
   - 创建 Command 对象

3. 去重（通过 realpath 检测同一文件）

4. 分类：
   - 无条件技能：立即可用
   - 条件技能：匹配特定文件路径时才激活

5. 返回技能列表
```

关键代码片段：

```typescript
// loadSkillsFromSkillsDir —— 从目录加载技能
async function loadSkillsFromSkillsDir(basePath, source) {
  const entries = await fs.readdir(basePath)
  const results = await Promise.all(
    entries.map(async (entry) => {
      // 只支持目录格式：skill-name/SKILL.md
      const skillFilePath = join(skillDirPath, 'SKILL.md')
      const content = await fs.readFile(skillFilePath, 'utf-8')
      const { frontmatter, content: markdownContent } = parseFrontmatter(content)
      return createSkillCommand({ skillName: entry.name, markdownContent, ... })
    })
  )
  return results.filter(r => r !== null)
}
```

### 技能激活

参考 `src/skills/bundledSkills.ts`，技能被调用时：

```typescript
// getPromptForCommand —— 技能被调用时生成 prompt
async getPromptForCommand(args, toolUseContext) {
  let finalContent = baseDir
    ? `Base directory for this skill: ${baseDir}\n\n${markdownContent}`
    : markdownContent

  // 替换参数占位符
  finalContent = substituteArguments(finalContent, args, true, argumentNames)

  // 替换 ${CLAUDE_SKILL_DIR} 为技能目录路径
  finalContent = finalContent.replace(/\$\{CLAUDE_SKILL_DIR\}/g, skillDir)

  return [{ type: 'text', text: finalContent }]
}
```

### 内置技能

Claude Code 内置了丰富的技能（`src/skills/bundled/`）：

| 技能 | 用途 |
|------|------|
| `remember` | 记住重要信息 |
| `verify` | 验证代码正确性 |
| `loop` | 循环执行直到成功 |
| `batch` | 批量处理 |
| `simplify` | 简化复杂代码 |
| `claudeApi` | 直接调用 Claude API |
| `stuck` | 检测和处理卡住的情况 |

---

## 🧠 核心概念

### 技能系统架构

```
┌─────────────────────────────────────────────┐
│              用户指令                         │
│         "/code-review src/main.py"          │
├──────────────────┬──────────────────────────┤
│   技能解析器       │   技能加载器              │
│   - 解析指令       │   - 扫描技能目录          │
│   - 匹配技能       │   - 解析 SKILL.md        │
│   - 提取参数       │   - 去重 & 分类          │
├──────────────────┴──────────────────────────┤
│              技能执行引擎                     │
│   1. 注入技能 prompt 到系统消息              │
│   2. 替换参数占位符                          │
│   3. 注入技能目录上下文                       │
│   4. Agent 按技能指导执行                    │
├─────────────────────────────────────────────┤
│              技能文件                         │
│   SKILL.md = YAML frontmatter + Markdown    │
│   - name: 技能名                             │
│   - description: 描述                        │
│   - when_to_use: 何时使用                    │
│   - 正文: 详细指导                           │
└─────────────────────────────────────────────┘
```

### 技能 vs 工具（Skills vs Tools）

| 维度 | 工具（Tools） | 技能（Skills） |
|------|-------------|--------------|
| 本质 | 代码函数 | Markdown 文档 |
| 能力 | 执行操作（读写文件、运行命令） | 指导行为（如何思考、如何执行） |
| 扩展 | 需要写代码 | 只需要写 Markdown |
| 粒度 | 原子操作 | 复合流程 |
| 示例 | Read, Write, Bash | 代码审查、文章写作 |

> **关键理解**：技能不是代码，是**提示词工程的结构化封装**。它把最佳实践、工作流程、检查清单打包成一个可复用的模块。

### Frontmatter 字段详解

| 字段 | 类型 | 说明 |
|------|------|------|
| `name` | string | 技能名称（默认用目录名） |
| `description` | string | 简短描述（必须） |
| `when_to_use` | string | 何时使用此技能 |
| `allowed-tools` | list | 允许使用的工具列表 |
| `argument-hint` | string | 参数提示 |
| `arguments` | list | 参数名列表 |
| `model` | string | 指定使用的模型 |
| `user-invocable` | bool | 用户是否可直接调用 |
| `context` | string | 执行上下文（inline/fork） |
| `paths` | list | 条件激活路径模式 |

---

## 💻 动手实现

### 项目结构

```
skills/
├── skill.py            # Skill 类：解析、表示、执行
├── loader.py           # SkillLoader：扫描、加载、去重
└── __init__.py
my-skills/
├── code-review/
│   └── SKILL.md
├── article-writing/
│   └── SKILL.md
└── data-analysis/
    └── SKILL.md
```

### 1. 技能类 `skills/skill.py`

```python
"""
skills/skill.py —— 技能表示与执行

参考 Claude Code 的 Command / createSkillCommand / parseSkillFrontmatterFields
"""

import re
import logging
from dataclasses import dataclass, field
from typing import Optional
from pathlib import Path

logger = logging.getLogger(__name__)


@dataclass
class SkillFrontmatter:
    """
    技能 frontmatter（YAML 元数据）

    参考 Claude Code 的 FrontmatterData / parseSkillFrontmatterFields
    """
    name: Optional[str] = None
    description: str = ""
    when_to_use: Optional[str] = None
    allowed_tools: list[str] = field(default_factory=list)
    argument_hint: Optional[str] = None
    arguments: list[str] = field(default_factory=list)
    model: Optional[str] = None
    user_invocable: bool = True
    context: Optional[str] = None  # "inline" or "fork"
    paths: list[str] = field(default_factory=list)  # 条件激活路径
    version: Optional[str] = None

    @classmethod
    def from_dict(cls, data: dict) -> "SkillFrontmatter":
        """从字典创建"""
        # 解析 allowed-tools（可能是逗号分隔字符串或列表）
        tools = data.get("allowed-tools", [])
        if isinstance(tools, str):
            tools = [t.strip() for t in tools.split(",")]

        # 解析 arguments（可能是逗号分隔字符串或列表）
        args = data.get("arguments", [])
        if isinstance(args, str):
            args = [a.strip() for a in args.split(",")]

        # 解析 paths
        paths = data.get("paths", [])
        if isinstance(paths, str):
            paths = [p.strip() for p in paths.split(",")]

        return cls(
            name=data.get("name"),
            description=data.get("description", ""),
            when_to_use=data.get("when_to_use"),
            allowed_tools=tools,
            argument_hint=data.get("argument-hint"),
            arguments=args,
            model=data.get("model"),
            user_invocable=data.get("user-invocable", True),
            context=data.get("context"),
            paths=paths,
            version=data.get("version"),
        )


def parse_frontmatter(content: str) -> tuple[SkillFrontmatter, str]:
    """
    解析 SKILL.md 内容，分离 frontmatter 和正文

    参考 Claude Code 的 parseFrontmatter

    格式：
    ---
    name: my-skill
    description: My skill
    ---
    # Skill content starts here
    ...
    """
    # 简单的 YAML frontmatter 解析
    if not content.startswith("---"):
        # 没有 frontmatter，整个文件都是正文
        fm = SkillFrontmatter()
        return fm, content

    # 找到第二个 ---
    parts = content.split("---", 2)
    if len(parts) < 3:
        fm = SkillFrontmatter()
        return fm, content

    yaml_text = parts[1].strip()
    markdown_text = parts[2].strip()

    # 解析 YAML（简单实现，不依赖 pyyaml）
    fm_dict = {}
    for line in yaml_text.split("\n"):
        line = line.strip()
        if not line or line.startswith("#"):
            continue
        if ":" in line:
            key, _, value = line.partition(":")
            key = key.strip()
            value = value.strip().strip("'").strip('"')
            # 尝试类型转换
            if value.lower() == "true":
                value = True
            elif value.lower() == "false":
                value = False
            elif value.isdigit():
                value = int(value)
            fm_dict[key] = value

    fm = SkillFrontmatter.from_dict(fm_dict)
    return fm, markdown_text


@dataclass
class Skill:
    """
    技能表示（参考 Claude Code 的 Command）

    一个技能 = frontmatter 元数据 + Markdown 正文 + 可选的参考文件
    """
    name: str
    description: str
    frontmatter: SkillFrontmatter
    content: str  # Markdown 正文
    base_dir: Optional[Path] = None  # 技能目录
    source: str = "user"  # "user" | "project" | "bundled" | "plugin"
    is_hidden: bool = False

    @classmethod
    def from_file(cls, file_path: Path) -> Optional["Skill"]:
        """
        从 SKILL.md 文件加载技能

        参考 Claude Code 的 loadSkillsFromSkillsDir
        """
        try:
            content = file_path.read_text(encoding="utf-8")
        except Exception as e:
            logger.warning(f"Failed to read skill file {file_path}: {e}")
            return None

        frontmatter, markdown_content = parse_frontmatter(content)

        # 技能名优先用 frontmatter，其次用目录名
        skill_name = frontmatter.name or file_path.parent.name

        return cls(
            name=skill_name,
            description=frontmatter.description or skill_name,
            frontmatter=frontmatter,
            content=markdown_content,
            base_dir=file_path.parent,
        )

    def build_prompt(self, args: str = "") -> str:
        """
        构建技能 prompt（参考 Claude Code 的 getPromptForCommand）

        1. 添加技能目录路径
        2. 替换参数占位符
        3. 替换特殊变量
        """
        parts = []

        # 添加基础目录信息
        if self.base_dir:
            parts.append(f"Base directory for this skill: {self.base_dir}\n")

        # 添加技能内容
        prompt = self.content

        # 替换参数占位符 {arg_name}
        if args and self.frontmatter.arguments:
            arg_values = self._parse_args(args)
            for i, arg_name in enumerate(self.frontmatter.arguments):
                placeholder = f"{{{arg_name}}}"
                value = arg_values[i] if i < len(arg_values) else ""
                prompt = prompt.replace(placeholder, value)

        # 替换 ${SKILL_DIR} 变量
        if self.base_dir:
            prompt = prompt.replace("${SKILL_DIR}", str(self.base_dir))

        parts.append(prompt)
        return "\n".join(parts)

    def _parse_args(self, args_str: str) -> list[str]:
        """解析参数字符串"""
        # 简单实现：按空格分割，支持引号
        import shlex
        try:
            return shlex.split(args_str)
        except ValueError:
            return args_str.split()

    def matches_path(self, file_path: str) -> bool:
        """
        检查文件路径是否匹配此技能的条件路径

        参考 Claude Code 的 activateConditionalSkillsForPaths
        """
        if not self.frontmatter.paths:
            return True

        for pattern in self.frontmatter.paths:
            if self._match_glob(pattern, file_path):
                return True
        return False

    def _match_glob(self, pattern: str, file_path: str) -> bool:
        """简单的 glob 匹配"""
        import fnmatch
        return fnmatch.fnmatch(file_path, pattern)

    def __repr__(self) -> str:
        return f"Skill({self.name!r}, {self.description[:50]!r})"
```

### 2. 技能加载器 `skills/loader.py`

```python
"""
skills/loader.py —— 技能加载与管理

参考 Claude Code 的 loadSkillsDir / getSkillDirCommands / bundledSkills
"""

import logging
from pathlib import Path
from typing import Optional

from .skill import Skill

logger = logging.getLogger(__name__)


class SkillLoader:
    """
    技能加载器

    参考 Claude Code 的技能加载流程：
    1. 扫描多个技能目录
    2. 解析每个 SKILL.md
    3. 去重（通过 realpath）
    4. 分类（无条件 / 条件）
    5. 返回可用技能列表
    """

    def __init__(self):
        self._skills: dict[str, Skill] = {}
        self._conditional_skills: dict[str, Skill] = {}
        self._skill_dirs_checked: set[str] = set()

    def load_directory(self, skills_dir: Path, source: str = "user") -> list[Skill]:
        """
        从目录加载所有技能

        参考 Claude Code 的 loadSkillsFromSkillsDir：
        - 只支持目录格式：skill-name/SKILL.md
        - 不支持单个 .md 文件
        """
        if not skills_dir.exists():
            logger.debug(f"Skills directory not found: {skills_dir}")
            return []

        skills = []
        for entry in sorted(skills_dir.iterdir()):
            if not entry.is_dir():
                continue

            skill_file = entry / "SKILL.md"
            if not skill_file.exists():
                continue

            skill = Skill.from_file(skill_file)
            if skill:
                skill.source = source
                skills.append(skill)
                logger.debug(f"Loaded skill: {skill.name}")

        return skills

    def load_all(
        self,
        user_dir: Optional[Path] = None,
        project_dir: Optional[Path] = None,
        bundled_dir: Optional[Path] = None,
    ) -> dict[str, Skill]:
        """
        从所有目录加载技能（参考 Claude Code 的 getSkillDirCommands）

        优先级：bundled < user < project（后面的覆盖前面的）
        """
        all_skills: list[tuple[str, Skill]] = []

        # 1. 内置技能
        if bundled_dir:
            for skill in self.load_directory(bundled_dir, "bundled"):
                all_skills.append(("bundled", skill))

        # 2. 用户技能
        if user_dir:
            for skill in self.load_directory(user_dir, "user"):
                all_skills.append(("user", skill))

        # 3. 项目技能
        if project_dir:
            for skill in self.load_directory(project_dir, "project"):
                all_skills.append(("project", skill))

        # 去重（后面的覆盖前面的）
        self._skills.clear()
        self._conditional_skills.clear()

        for source, skill in all_skills:
            if skill.frontmatter.paths:
                # 条件技能：暂不激活
                self._conditional_skills[skill.name] = skill
            else:
                self._skills[skill.name] = skill

        logger.info(
            f"Loaded {len(self._skills)} skills "
            f"({len(self._conditional_skills)} conditional)"
        )
        return dict(self._skills)

    def get(self, name: str) -> Optional[Skill]:
        """获取技能"""
        return self._skills.get(name)

    def list_skills(self) -> list[dict]:
        """列出所有可用技能"""
        return [
            {
                "name": s.name,
                "description": s.description,
                "when_to_use": s.frontmatter.when_to_use,
                "source": s.source,
            }
            for s in sorted(self._skills.values(), key=lambda x: x.name)
        ]

    def activate_conditional(self, file_path: str) -> list[str]:
        """
        激活匹配文件路径的条件技能

        参考 Claude Code 的 activateConditionalSkillsForPaths
        """
        activated = []
        for name, skill in list(self._conditional_skills.items()):
            if skill.matches_path(file_path):
                self._skills[name] = skill
                del self._conditional_skills[name]
                activated.append(name)
                logger.info(f"Activated conditional skill: {name}")
        return activated

    def search(self, query: str) -> list[Skill]:
        """
        搜索技能（按描述匹配）

        用于 Agent 自动发现相关技能。
        """
        query_lower = query.lower()
        results = []
        for skill in self._skills.values():
            if (
                query_lower in skill.name.lower()
                or query_lower in skill.description.lower()
                or (skill.frontmatter.when_to_use
                    and query_lower in skill.frontmatter.when_to_use.lower())
            ):
                results.append(skill)
        return results


# --- 便捷函数 ---

def load_skills_from_dir(skills_dir: Path) -> list[Skill]:
    """快速加载单个目录的技能"""
    loader = SkillLoader()
    return loader.load_directory(skills_dir)
```

### 3. 实战：编写 3 个技能

#### 技能 1：代码审查 `my-skills/code-review/SKILL.md`

```markdown
---
name: code-review
description: Perform thorough code review with security and quality checks
when_to_use: When reviewing code changes, pull requests, or code quality audits
allowed-tools: Read, Grep, Glob, Bash
arguments: target
argument-hint: <file-or-directory>
---

# Code Review Skill

You are a senior code reviewer with 15+ years of experience.

## Review Process

When invoked, follow these steps:

### Step 1: Understand the Code
- Read the target file(s) or directory
- Understand the purpose and context
- Identify the language and framework

### Step 2: Security Review
Check for:
- SQL injection vulnerabilities
- XSS vulnerabilities
- Hardcoded secrets or credentials
- Insecure deserialization
- Path traversal issues
- Race conditions

### Step 3: Code Quality
Check for:
- DRY violations (duplicated code)
- Function length (>50 lines is a smell)
- Cyclomatic complexity
- Error handling completeness
- Naming conventions
- Type safety

### Step 4: Architecture Review
Check for:
- Separation of concerns
- Dependency direction
- Interface design
- Test coverage gaps

### Step 5: Output Report

Format your review as:

```
## Code Review: {target}

### 🔴 Critical Issues
- [List security or correctness issues]

### 🟡 Warnings
- [List code quality concerns]

### 🟢 Suggestions
- [List improvements]

### ✅ Good Practices
- [List things done well]
```

## Rules

- Be specific: quote the problematic code and line numbers
- Be constructive: suggest concrete fixes, not just problems
- Prioritize: Critical > Warning > Suggestion
- Don't nitpick: Focus on meaningful issues
```

#### 技能 2：文章写作 `my-skills/article-writing/SKILL.md`

```markdown
---
name: article-writing
description: Write high-quality articles with proper structure and engaging content
when_to_use: When writing blog posts, documentation, or any long-form content
allowed-tools: Read, Write, Grep
arguments: topic tone length
argument-hint: <topic> [tone] [length]
---

# Article Writing Skill

You are a professional writer and editor.

## Writing Process

### Step 1: Research
- Understand the topic deeply
- Identify the target audience
- Determine the key message

### Step 2: Outline
Create a structured outline:
- Catchy title
- Hook (opening paragraph)
- Main sections (3-5)
- Conclusion with call-to-action

### Step 3: Draft
Write the article following these principles:
- **Hook first**: Grab attention in the first 2 sentences
- **Short paragraphs**: Max 3-4 sentences each
- **Active voice**: Prefer "we built" over "was built"
- **Concrete examples**: Show, don't tell
- **Subheadings**: Every 2-3 paragraphs

### Step 4: Review
Check for:
- Logical flow between sections
- Consistent tone ({tone})
- No jargon without explanation
- Proper formatting (headers, lists, code blocks)
- Word count target: {length}

## Output Format

Write the article in Markdown format:

```markdown
# Title

> A compelling subtitle/hook

## Section 1
Content...

## Section 2
Content...

## Conclusion
Wrap up + call to action
```

## Tone Options

- **professional**: Formal, authoritative, data-driven
- **casual**: Conversational, friendly, relatable
- **technical**: Precise, detailed, code-heavy
- **storytelling**: Narrative-driven, personal, emotional

Default tone: professional
Default length: 1500 words
```

#### 技能 3：数据分析 `my-skills/data-analysis/SKILL.md`

```markdown
---
name: data-analysis
description: Analyze data files and generate insights with visualizations
when_to_use: When working with CSV, JSON, or other data files that need analysis
allowed-tools: Read, Write, Bash
arguments: file_path analysis_type
argument-hint: <file> [summary|trends|correlations|anomalies]
---

# Data Analysis Skill

You are a data scientist with expertise in exploratory data analysis.

## Analysis Process

### Step 1: Data Understanding
- Read the data file ({file_path})
- Identify file format (CSV, JSON, Excel, etc.)
- Check schema: columns, types, missing values
- Report basic stats: row count, column count

### Step 2: Data Cleaning
- Handle missing values
- Remove duplicates
- Fix data types
- Note any anomalies found

### Step 3: Analysis ({analysis_type})

**summary**: Basic statistics for each column
- Mean, median, std, min, max for numeric
- Value counts for categorical

**trends**: Time series analysis
- Identify patterns over time
- Seasonality detection
- Growth rates

**correlations**: Relationship analysis
- Correlation matrix
- Key relationships
- Scatter plot suggestions

**anomalies**: Outlier detection
- Statistical outliers (IQR, Z-score)
- Unexpected patterns
- Data quality issues

### Step 4: Visualization Plan

Suggest Python code for key visualizations:

```python
import matplotlib.pyplot as plt
import seaborn as sns

# Example: distribution plot
sns.histplot(data=df, x='column_name', kde=True)
plt.title('Distribution of Column Name')
plt.show()
```

### Step 5: Insights Report

Format findings as:

```
## Data Analysis Report: {file_path}

### Dataset Overview
- Rows: X, Columns: Y
- Missing values: Z%

### Key Findings
1. [Most important insight]
2. [Second most important]
3. [Third most important]

### Recommendations
- [Actionable recommendation 1]
- [Actionable recommendation 2]
```
```

---

## 🧪 测试验证

创建测试文件 `tests/test_skills.py`：

```python
"""
tests/test_skills.py —— 技能系统测试
"""

import pytest
import tempfile
from pathlib import Path
from skills.skill import Skill, SkillFrontmatter, parse_frontmatter
from skills.loader import SkillLoader


class TestSkill:
    """测试技能类"""

    def test_parse_frontmatter_basic(self):
        """测试基本 frontmatter 解析"""
        content = """---
name: test-skill
description: A test skill
when_to_use: For testing
---

# Test Skill

This is the skill content.
"""
        fm, body = parse_frontmatter(content)
        assert fm.name == "test-skill"
        assert fm.description == "A test skill"
        assert fm.when_to_use == "For testing"
        assert "This is the skill content" in body

    def test_parse_frontmatter_no_frontmatter(self):
        """测试没有 frontmatter 的情况"""
        content = "# Just content\n\nNo frontmatter here."
        fm, body = parse_frontmatter(content)
        assert fm.name is None
        assert body == content

    def test_skill_from_file(self, tmp_path):
        """测试从文件加载技能"""
        skill_dir = tmp_path / "my-skill"
        skill_dir.mkdir()
        skill_file = skill_dir / "SKILL.md"
        skill_file.write_text("""---
name: my-skill
description: My awesome skill
allowed-tools: Read, Write
---

# My Skill

Do great things.
""")
        skill = Skill.from_file(skill_file)
        assert skill is not None
        assert skill.name == "my-skill"
        assert skill.description == "My awesome skill"
        assert skill.base_dir == skill_dir
        assert "Do great things" in skill.content

    def test_skill_build_prompt(self, tmp_path):
        """测试构建 prompt"""
        skill_dir = tmp_path / "test-skill"
        skill_dir.mkdir()
        skill_file = skill_dir / "SKILL.md"
        skill_file.write_text("""---
name: test-skill
description: Test
arguments: filename
---

# Test Skill

Analyze the file: {filename}
Directory: ${SKILL_DIR}
""")
        skill = Skill.from_file(skill_file)
        prompt = skill.build_prompt("data.csv")
        assert "Base directory for this skill:" in prompt
        assert "Analyze the file: data.csv" in prompt
        assert str(skill_dir) in prompt

    def test_skill_matches_path(self):
        """测试路径匹配"""
        fm = SkillFrontmatter(paths=["*.py", "src/**/*.js"])
        skill = Skill(
            name="test", description="test",
            frontmatter=fm, content=""
        )
        assert skill.matches_path("main.py")
        assert skill.matches_path("src/app.js")
        assert not skill.matches_path("README.md")


class TestSkillLoader:
    """测试技能加载器"""

    def test_load_directory(self, tmp_path):
        """测试从目录加载"""
        # 创建技能目录结构
        skill1_dir = tmp_path / "code-review"
        skill1_dir.mkdir()
        (skill1_dir / "SKILL.md").write_text("""---
name: code-review
description: Review code
---
# Code Review
""")

        skill2_dir = tmp_path / "data-analysis"
        skill2_dir.mkdir()
        (skill2_dir / "SKILL.md").write_text("""---
name: data-analysis
description: Analyze data
---
# Data Analysis
""")

        # 非技能目录（没有 SKILL.md）
        (tmp_path / "not-a-skill").mkdir()

        loader = SkillLoader()
        skills = loader.load_directory(tmp_path)
        assert len(skills) == 2
        names = {s.name for s in skills}
        assert names == {"code-review", "data-analysis"}

    def test_load_all_with_priority(self, tmp_path):
        """测试多目录加载（优先级覆盖）"""
        # 用户目录
        user_dir = tmp_path / "user-skills"
        user_dir.mkdir()
        (user_dir / "review" / "SKILL.md").parent.mkdir(parents=True)
        (user_dir / "review" / "SKILL.md").write_text("""---
name: review
description: User version
---
User review skill
""")

        # 项目目录（同名技能，应覆盖）
        project_dir = tmp_path / "project-skills"
        project_dir.mkdir()
        (project_dir / "review" / "SKILL.md").parent.mkdir(parents=True)
        (project_dir / "review" / "SKILL.md").write_text("""---
name: review
description: Project version
---
Project review skill
""")

        loader = SkillLoader()
        skills = loader.load_all(
            user_dir=user_dir,
            project_dir=project_dir,
        )
        assert skills["review"].description == "Project version"
        assert skills["review"].source == "project"

    def test_conditional_skills(self, tmp_path):
        """测试条件技能"""
        skills_dir = tmp_path / "skills"
        skills_dir.mkdir()
        (skills_dir / "python-review" / "SKILL.md").parent.mkdir(parents=True)
        (skills_dir / "python-review" / "SKILL.md").write_text("""---
name: python-review
description: Review Python code
paths: "*.py, src/**/*.py"
---
# Python Review
""")

        loader = SkillLoader()
        skills = loader.load_all(user_dir=skills_dir)

        # 条件技能不应在初始列表中
        assert "python-review" not in skills

        # 激活匹配的技能
        activated = loader.activate_conditional("src/main.py")
        assert "python-review" in activated

        # 现在应该能获取到
        assert loader.get("python-review") is not None

    def test_search_skills(self, tmp_path):
        """测试技能搜索"""
        skills_dir = tmp_path / "skills"
        skills_dir.mkdir()

        for name, desc in [
            ("code-review", "Review code quality"),
            ("security-audit", "Find security vulnerabilities"),
            ("data-analysis", "Analyze datasets"),
        ]:
            (skills_dir / name / "SKILL.md").parent.mkdir(parents=True)
            (skills_dir / name / "SKILL.md").write_text(f"""---
name: {name}
description: {desc}
---
Content
""")

        loader = SkillLoader()
        loader.load_all(user_dir=skills_dir)

        results = loader.search("security")
        assert len(results) == 1
        assert results[0].name == "security-audit"

        results = loader.search("code")
        assert len(results) == 1  # code-review matches
```

运行测试：

```bash
pytest tests/test_skills.py -v
```

预期输出：

```
tests/test_skills.py::TestSkill::test_parse_frontmatter_basic PASSED
tests/test_skills.py::TestSkill::test_parse_frontmatter_no_frontmatter PASSED
tests/test_skills.py::TestSkill::test_skill_from_file PASSED
tests/test_skills.py::TestSkill::test_skill_build_prompt PASSED
tests/test_skills.py::TestSkill::test_skill_matches_path PASSED
tests/test_skills.py::TestSkillLoader::test_load_directory PASSED
tests/test_skills.py::TestSkillLoader::test_load_all_with_priority PASSED
tests/test_skills.py::TestSkillLoader::test_conditional_skills PASSED
tests/test_skills.py::TestSkillLoader::test_search_skills PASSED
```

---

## 📝 本章小结

本章我们深入学习了 Claude Code 的技能系统，并实现了完整的技能框架：

| 概念 | Claude Code 实现 | 我们的 Python 实现 |
|------|-----------------|-------------------|
| 技能文件 | SKILL.md（frontmatter + Markdown） | 相同的格式 |
| 解析器 | parseFrontmatter | parse_frontmatter |
| 技能表示 | Command 对象 | Skill 类 |
| 加载器 | loadSkillsFromSkillsDir | SkillLoader.load_directory |
| 多源加载 | managed + user + project + bundled | 相同的多源加载 |
| 去重 | realpath 检测 | 字典覆盖 |
| 条件技能 | paths frontmatter + activateConditionalSkillsForPaths | 相同的条件激活 |
| 参数替换 | substituteArguments | build_prompt 中的替换 |
| 变量注入 | ${CLAUDE_SKILL_DIR} | ${SKILL_DIR} |

**核心收获：**

1. **技能 = 结构化提示词**——SKILL.md 本质是把最佳实践编码成可复用的 Markdown 文档
2. **Frontmatter 是元数据层**——它让技能可搜索、可过滤、可条件激活
3. **目录即命名空间**——目录名就是技能名，简单而有效
4. **条件激活是杀手级特性**——操作 Python 文件时自动激活 Python 审查技能

---

## 🏋️ 课后练习

### 练习 1：实现技能版本管理

> **提示**：在 Skill 类中添加 `version` 字段，实现技能版本检查。当技能版本低于要求时发出警告。参考 Claude Code 的 `version` frontmatter 字段。

### 练习 2：实现技能依赖

> **提示**：在 frontmatter 中添加 `requires` 字段，指定依赖的其他技能。加载时检查依赖是否满足。可以用拓扑排序解析依赖图。

### 练习 3：实现技能测试框架

> **提示**：在 SKILL.md 中添加 `tests` 部分，定义输入/输出对。编写一个测试运行器，验证技能是否能正确处理各种输入。

### 练习 4：实现技能市场（Skill Hub）

> **提示**：设计一个技能索引文件格式 `skills-index.json`，包含技能名、描述、作者、下载 URL。实现技能的在线搜索和安装功能。

**答案提示：**
- 练习 1：用 `packaging.version` 模块比较版本号
- 练习 2：`networkx` 库做拓扑排序，检测循环依赖
- 练习 3：在 SKILL.md 中添加 `## Tests` 部分，用 YAML 格式定义测试用例
- 练习 4：参考 npm/pip 的包索引设计，用 JSON 存储元数据

---

> 📖 **上一章**：[第 7 章：任务系统 —— 让 AI 同时做多件事](ch07-任务系统.md)
>
> 📖 **下一章**：[第 9 章：MCP 协议 —— 让 AI 连接一切](ch09-MCP协议.md)
